# Install pangu-sdk as a real folder rather than a symlink. A symlink would let
# node resolve @solana/web3.js inside packages/sdk as well as here, and two
# copies of PublicKey are not the same class, so every input check would fail.
install-links=true
